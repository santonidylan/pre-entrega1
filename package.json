{
  "name": "shipnow-api",
  "version": "1.0.0",
  "description": "API de ShipNow refactorizada en arquitectura por capas (Controller-Service-Repository) con configuración de entorno validada.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "cross-env NODE_ENV=test mocha src/tests/**/*.test.js --timeout 10000 --exit"
  },
  "dependencies": {
    "@faker-js/faker": "^10.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.0",
    "multer": "^2.3.0",
    "swagger-jsdoc": "^6.3.0",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "cross-env": "^10.1.0",
    "mocha": "^12.0.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.2.2"
  }
}
